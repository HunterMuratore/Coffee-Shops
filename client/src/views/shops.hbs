<section class="shops">
    <h1 class="text-center">Shops</h1>

    {{#each shops}}
        <div class="shop">
            <h3>{{name}}</h3>
            <p>Location: {{location}}</p>
        </div>
    {{/each}}
</section>